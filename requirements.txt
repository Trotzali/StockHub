# requirements.txt -- direct dependencies pinned for StockHub.
#
# WP-INFRA-REQUIREMENTS-PIN replaces the previous lockfile-style
# transitive bloat (54 lines from 1ed6d8b WP-DEV-ENV-SETUP) with a
# clean direct-deps-only manifest. Transitive deps are left to pip
# resolution. Pinned versions are the verified working set installed
# in .venv at the time of this commit.
#
# For exact reproducible installs (the ARM64-validated transitive
# snapshot from 1ed6d8b), use requirements.lock:
#     python -m pip install --only-binary :all: -r requirements.lock
# The lock holds the full transitive closure. Refresh it via
#     python -m pip freeze > requirements.lock
# whenever a pin in this file changes (and re-verify the working set
# against current-stable on PyPI before committing the refreshed lock).
#
# Install policy (Windows 11 ARM64 / Snapdragon, per CLAUDE.md):
#     python -m pip install --only-binary :all: -r requirements.txt
# Sdist fallback fails on this box (no native build toolchain), so
# always require binary wheels. Verify resolved versions vs current-
# stable before bumping a pin -- pip's exit 0 is necessary but NOT
# sufficient (silent resolver back-walk is a known failure mode).
#
# Each pin below has at least one direct `import` or `from` statement
# in src/ or scripts/ at this commit. beautifulsoup4/requests/plotly/
# streamlit are intentionally absent: not directly imported; pip will
# pull required transitives.

httpx==0.28.1
numpy==2.4.5
pandas==3.0.3
python-dotenv==1.2.2
supabase==2.25.1
yfinance==1.3.0
